import React from 'react';
import NavBar from '../../NavBar/NavBar';
import { Outlet } from 'react-router';

const Home = () => {
    return (
        <div>
            <p>This is Home</p>
        </div>
    );
};

export default Home;